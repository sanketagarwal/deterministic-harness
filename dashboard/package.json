{
  "name": "deterministic-harness-dashboard",
  "version": "1.0.0",
  "description": "Notion-backed UI for managing deterministic harness pipelines",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup": "node notion/setup-databases.js"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.0",
    "express": "^4.18.0",
    "dotenv": "^16.4.0"
  }
}
