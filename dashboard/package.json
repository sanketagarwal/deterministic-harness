{
  "name": "deterministic-harness-tracking",
  "version": "1.0.0",
  "description": "Local JSON and optional Notion tracking for deterministic harness pipelines",
  "scripts": {
    "setup-notion": "node notion/setup-databases.js",
    "export-csv": "node local/csv-export.js"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.0",
    "dotenv": "^16.4.0"
  }
}
